spring:
  application:
    name: appointment-ms

  profiles:
    default: prod

  config:
    import: "optional:configserver:${CONFIG_SERVER_HOST:http://localhost:7777}"

  cloud:
    stream:
      kafka:
        binder:
          brokers: ${KAFKA_BROKER:kafka-hospital:29092}

      bindings:
      
        appointment-created-out-0:
          destination: appointment-created
          content-type: application/json

        appointment-updated-status-out-0:
          destination: appointment-updated-status
          content-type: application/json

        appointment-created-type-out-0:
          destination: appointment-created-type
          content-type: application/json


        doctorCreatedConsumer-in-0:
          destination: doctor-created
          group: appointment-ms-group
          content-type: application/json
        doctorUpdatedConsumer-in-0:
          destination: doctor-updated
          group: appointment-ms-group
          content-type: application/json

        patientCreatedConsumer-in-0:
          destination: patient-created
          group: appointment-ms-group
          content-type: application/json
        patientUpdatedConsumer-in-0:
          destination: patient-updated
          group: appointment-ms-group
          content-type: application/json
    openfeign:
      circuitbreaker:
        enabled: true


  datasource:
    username: ${db.username:postgres}
    password: ${db.password:123456}
    url: ${db.url:jdbc:postgresql://localhost:5434/hospital_db}
    hikari:
      connection-timeout: ${db.connection-timeout:30000}
      maximum-pool-size: ${db.maximum-pool-size:10}

resilience4j:
  circuitbreaker:

    configs:
      default:
        slidingWindowType: COUNT_BASED
        slidingWindowSize: 5
        minimumNumberOfCalls: 5
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
        permittedNumberOfCallsInHalfOpenState: 2

    instances:

      patient-ms:
        baseConfig: default
        ignoreExceptions:
          - personal.shared.exception.BusinessException
          - feign.FeignException.BadRequest
          - feign.FeignException.NotFound
          - feign.FeignException.UnprocessableEntity

      doctor-ms:
        baseConfig: default
        ignoreExceptions:
          - personal.shared.exception.BusinessException
          - feign.FeignException.BadRequest
          - feign.FeignException.NotFound
          - feign.FeignException.UnprocessableEntity
server:
  port: 8081
  servlet:
    context-path: /appointments


eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}

  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://ms-registry-server:8761/eureka}


management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics


scalar:
  enabled: false
  darkMode: true
  theme: deepSpace