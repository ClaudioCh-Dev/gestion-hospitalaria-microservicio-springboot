<configuration>

    <conversionRule
            conversionWord="clr"
            converterClass="org.springframework.boot.logging.logback.ColorConverter"/>


    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{HH:mm:ss.SSS}){faint} 
                     %highlight(%-5level) 
                     %clr([%15.15thread]){blue} 
                     %clr(%logger{36}){cyan} 
                     %clr([traceId=%X{traceId}]){yellow} 
                     - %msg%n"/>


    <appender name="CONSOLE"
              class="ch.qos.logback.core.ConsoleAppender">

        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>

    </appender>


    <!-- Desarrollo -->
    <springProfile name="default">

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>

    </springProfile>


    <!-- Gateway con seguridad OAuth2/JWT -->
    <springProfile name="oauth2">

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>

    </springProfile>


    <!-- Producción -->
    <springProfile name="prod">

        <root level="WARN">
            <appender-ref ref="CONSOLE"/>
        </root>

    </springProfile>


</configuration>